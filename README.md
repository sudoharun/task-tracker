# Task Tracker

An app that tracks your tasks

## Installation

```
cd app
python -m venv .venv
pip install -r requirements.txt # If on Windows, this may not work. Instead try pip install pyside6 directly
```

## Usage
```
cd app
python main.py
```
